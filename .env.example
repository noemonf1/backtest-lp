# Copy to .env for local dev. On Railway, paste values into the
# Variables tab (never commit real keys).

# Free key from https://thegraph.com/studio/
# Required for "Fetch swap events" in the sidebar. The app runs without
# it -- the button just errors out with a clear message.
THEGRAPH_API_KEY=
